# -------------------------------
# .gitignore (NYFITCOACH_BOT)
# -------------------------------

# ① ─── 환경 변수 / 보안 파일 ─────────────────────
.env                      # API 키, 토큰 등 민감 정보

# ② ─── 캐시 / 컴파일 파일 ────────────────────────
__pycache__/              # 파이썬 캐시 폴더
*.pyc                     # 파이썬 바이트코드
*.pyo                     # 최적화된 바이트코드
*.pyd                     # 윈도우용 확장 모듈

# ③ ─── IDE / 개발 도구 관련 파일 ─────────────────
.vscode/                  # VSCode 설정
.idea/                    # PyCharm / IntelliJ 설정
*.swp                     # Vim 임시파일
.DS_Store                 # macOS Finder 설정

# ④ ─── 데이터 / 로그 ────────────────────────────
data/users.json           # 유저 개인 데이터 저장 파일
logs/                     # 로그 폴더 (추가될 경우)

# ⑤ ─── 기타 임시 파일 ────────────────────────────
*.log                     # 로그 파일
*.tmp                     # 임시 파일
